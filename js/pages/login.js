/* ==========================================================================
   Login — page logic
   Owner: Earl (Phase 1)
   No sidebar/header on this screen — it is the entry point before the app
   shell applies. Build real auth-simulation logic here in Phase 1.
   ========================================================================== */

document.addEventListener('DOMContentLoaded', () => {
  // TODO (Phase 1): demo login validation + redirect to dashboard.html
});
