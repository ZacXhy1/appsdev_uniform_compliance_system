/* ==========================================================================
   Detections — page logic
   Owner: Zachary (Phase 4)
   ========================================================================== */

document.addEventListener('DOMContentLoaded', () => {
  initShell('detections', 'Detections');

  // TODO (Phase 4): build out Detections using MOCK_DETECTIONS from
  // js/data/mock-data.js. See docs/PHASES.md for the deliverable.
});
