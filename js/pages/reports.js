/* ==========================================================================
   Reports — page logic
   Owner: Paul (Phase 6)
   ========================================================================== */

document.addEventListener('DOMContentLoaded', () => {
  initShell('reports', 'Reports');

  // TODO (Phase 6): build out Reports using MOCK_DETECTIONS from
  // js/data/mock-data.js. See docs/PHASES.md for the deliverable.
});
